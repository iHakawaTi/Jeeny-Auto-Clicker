import 'package:flutter/material.dart';

class AppColors {
  // Background & Surfaces
  static const Color background = Color(0xFF0D1117);
  static const Color surface = Color(0xFF161B22);
  static const Color cardBg = Color(0xFF21262D);
  static const Color cardBorder = Color(0xFF30363D);

  // Brand Accents
  static const Color primaryNeon = Color(0xFF00E676); // Vibrant Green for Active State
  static const Color primaryGlow = Color(0x3300E676);
  static const Color accentBlue = Color(0xFF29B6F6);

  // Status & Warnings
  static const Color warningRed = Color(0xFFFF5252);
  static const Color alertAmber = Color(0xFFFFB74D);

  // Typography
  static const Color textPrimary = Color(0xFFF0F6FC);
  static const Color textSecondary = Color(0xFF8B949E);
  static const Color textMuted = Color(0xFF484F58);
}
